[defaults]
inventory = inventory
private_key_file = ~/.ssh/ansible
interpreter_python = auto_silent
timeout = 60

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = True
timeout = 60
